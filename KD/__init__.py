"""KD — knowledge-distillation pipeline for HiVT.

This is a regular package. Import its modules absolutely from the repo root,
e.g. ``from KD.kd_dataset import KDDataset`` and run entrypoints as
``python -m KD.train_student_kd`` (or ``python KD/train_student_kd.py``; the
scripts bootstrap the repo root onto sys.path so both invocations work).
"""
